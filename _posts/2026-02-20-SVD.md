---
layout: default
title: 对 SVD 分解与 Ax=b 的思考
date: 2026-02-20
---

### 关于 SVD 分解

我的一点思考：我们暂且直接认为 $Ax=b$ (而不是靠估计什么的，就是完全相等)。我是这样想的，比如我记得 $V$ 对应的是 $A^TA$，基因之间的相关性，加上 $U$，那总的变化逻辑就是：先是有基因的相关性，然后又加上了他们的权重，最后给出了“在 $m$ 组情况下”得到的实验结果，也等于 $b$。

但是，$\Sigma$ 和 $x$ 不都代表权重吗？肯定不能是一个意思吧。让我先思考 $Ax=b$ 的意思。我认为它是“对指标权重做变换，变换的规则是在此变换下，按照 $x$ 的权重规则得到的理论观测值”。实际上 $x$ 和 $b$ 的“单位”不一样，不过我们可以在数字上把它们画在同一个坐标系下。

---

### $\Sigma$ 与 $x$ :

所以我再次思考 $\Sigma$ 和 $x$ 的区别。

$\Sigma, U, V$ 都是源自于 $A$，它是“从数字中的发现”，代表着“从 $A$ 矩阵的这一堆数字来看，如果我们采取 SVD 方法分解，一个确定的规律是某些新的基向量上延伸更长，也就意味着更重要”。

换句话说，你拿到任意一个矩阵，就有方法可以把它拆分成“正交1-对角-正交2”，其中对角（也就是 $\Sigma$ 里面的数据）体现的是正交2的“重要性”。正交2也就是 $V^T$，也就是表示的依据数据的指标权重。

我猜测这个的原则可能是变化越大，权重越大。比如 $A$ 的第一列完全相同，那就意味着这一个指标毫无作用，那么有可能它的 $\sigma$ 值很低。

而实际上 $x$ 只是整个 $R^n$ 空间中的一个取值，是主观的。非要比较的话，好比 $\Sigma$ 像是冷漠的**熵权法**，而 $x$ 像是 **AHP**（这里我是已知 $Ax$ 推 $b$ 的，虽然实际上一般是反过来，但是逻辑是一样的）。

另外，我猜测 $\Sigma$ 里面的数据可能和高斯分布的 $\sigma$ 有一定关系。

<script>
  window.MathJax = {
    tex: {
      inlineMath: [['$', '$'], ['\\(', '\\)']]
    }
  };
</script>
<script id="MathJax-script" async src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js"></script>
